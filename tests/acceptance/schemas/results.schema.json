{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PredictStructure results.json",
  "description": "v2.0 outputs map + UI-summary header. Run trace lives in metadata/metadata.json; this file is the entry point UIs and pipelines read to discover artifacts.",
  "type": "object",
  "required": ["schema_version", "state", "outputs"],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "2.0"
    },
    "state": {
      "type": "string",
      "enum": ["COMPLETED", "PARTIAL", "FAILED"],
      "description": "Outcome of the run."
    },
    "tool": {
      "type": "string",
      "description": "Convenience header — full trace is in metadata/metadata.json.tool."
    },
    "completed_at": {
      "type": "string",
      "format": "date-time"
    },
    "runtime_seconds": {
      "oneOf": [{"type": "number", "minimum": 0}, {"type": "null"}]
    },
    "metrics": {
      "type": "object",
      "description": "Scalar summary metrics. Per-residue/per-atom arrays stay in predictions/confidence.json.",
      "properties": {
        "plddt_mean": {"oneOf": [{"type": "number", "minimum": 0, "maximum": 100}, {"type": "null"}]},
        "ptm": {"oneOf": [{"type": "number", "minimum": 0, "maximum": 1}, {"type": "null"}]},
        "num_residues": {"oneOf": [{"type": "integer", "minimum": 0}, {"type": "null"}]},
        "num_atoms": {"oneOf": [{"type": "integer", "minimum": 0}, {"type": "null"}]}
      },
      "additionalProperties": true
    },
    "outputs": {
      "type": "object",
      "description": "Named output-port dictionary. Each value is a CWL File or Directory entry. Keys are stable port names (best_model, report, predictions, reports, inputs, metadata, raw).",
      "additionalProperties": {
        "oneOf": [
          {"$ref": "cwl-file.schema.json"},
          {"$ref": "cwl-directory.schema.json"}
        ]
      }
    }
  },
  "additionalProperties": true
}
